import React from 'react';

const Projects = () => (
  <section>
    <h2>Projects</h2>
    <p>Here, I would add some notable projects, such as backend applications built in Go, APIs, or other significant contributions.</p>
  </section>
);

export default Projects;
